import React from "react";
import BoardList from "../../components/board/BoardList.jsx";

const Boards = () => {
    return (
        <div>
            <BoardList/>
        </div>
    );
};

export default Boards;
